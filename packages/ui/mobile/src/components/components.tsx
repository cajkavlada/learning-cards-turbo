import { Text } from "react-native";

export const Pmobile = ({ children }) => (
  <Text className="bg-purple-600 text-lg leading-10 text-gray-800 dark:text-blue-200 md:text-xl">
    {children}
  </Text>
);
